import CalendarApp from '@/features/calendar/CalendarApp'

// Calendar module — Google Calendar-style with meetings, tasks, events,
// deadlines, company holidays, recurring series, drag-and-drop and
// Month / Week / Day / Agenda views.
export default function Calendar() {
  return <CalendarApp />
}
